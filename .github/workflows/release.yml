name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # for PyPI Trusted Publishing
      contents: write   # for GitHub Release
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
      - name: Set up Python
        run: uv python install 3.12
      - name: Sync deps
        run: uv sync --all-extras --locked
      - name: Verify version matches tag
        run: |
          PKG_VERSION=$(uv run python -c "from gangtise_openapi.__about__ import __version__; print(__version__)")
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          test "$PKG_VERSION" = "$TAG_VERSION" || (echo "Version mismatch: $PKG_VERSION vs $TAG_VERSION"; exit 1)
          grep -q "### ${TAG_VERSION} " README.md || (echo "README.md missing latest-version section for ${TAG_VERSION}"; exit 1)
          grep -q "## \\[${TAG_VERSION}\\]" CHANGELOG.md || (echo "CHANGELOG.md missing section for ${TAG_VERSION}"; exit 1)
      - name: Lint + type-check + test
        run: |
          uv run ruff check .
          uv run ruff format --check .
          uv run mypy src
          uv run pytest -m "not live"
      - name: Build
        run: uv build
      - name: Publish to PyPI
        # v1.14.2 — pinned by SHA. Must stay >= v1.14.2: it is the first release whose
        # bundled twine (v7) accepts core packaging metadata 2.5, which current
        # hatchling emits. v1.14.0 rejected the v0.3.0 wheel with
        # "InvalidDistribution: '2.5' is not a valid metadata version".
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
        with:
          packages-dir: dist/
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" --generate-notes dist/*
