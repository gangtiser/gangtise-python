name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # for PyPI Trusted Publishing
      contents: write   # for GitHub Release
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
      - name: Set up Python
        run: uv python install 3.12
      - name: Sync deps
        run: uv sync --all-extras --locked
      - name: Verify version matches tag
        run: |
          PKG_VERSION=$(uv run python -c "from gangtise_openapi.__about__ import __version__; print(__version__)")
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          test "$PKG_VERSION" = "$TAG_VERSION" || (echo "Version mismatch: $PKG_VERSION vs $TAG_VERSION"; exit 1)
          grep -q "### ${TAG_VERSION} " README.md || (echo "README.md missing latest-version section for ${TAG_VERSION}"; exit 1)
          grep -q "## \\[${TAG_VERSION}\\]" CHANGELOG.md || (echo "CHANGELOG.md missing section for ${TAG_VERSION}"; exit 1)
      - name: Lint + type-check + test
        run: |
          uv run ruff check .
          uv run ruff format --check .
          uv run mypy src
          uv run pytest -m "not live"
      - name: Build
        run: uv build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b
        with:
          packages-dir: dist/
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" --generate-notes dist/*
